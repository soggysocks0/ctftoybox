# Custom challenge — replace with whatever you need.
#
# Ideas:
#   - Crypto puzzle: a Python service that hands out an encrypted message
#     using a flawed crypto primitive
#   - OSINT: a static site with breadcrumbs in DNS / HTTP headers
#   - Misc: a service that requires creative protocol abuse to extract a flag

FROM alpine:3.19

WORKDIR /app
COPY flag.txt /flag.txt

# COPY your-source-here .

# EXPOSE <port>
CMD ["/bin/sh", "-c", "echo 'Replace this Dockerfile with your challenge.'; tail -f /dev/null"]
